services:
  mongo:
    image: mongo:7.0.21
    command: ["mongod", "--replSet", "rs0", "--bind_ip_all"]
    volumes:
      - mongo_data:/data/db
    healthcheck:
      test: ["CMD-SHELL", "mongosh --quiet --eval \"db.adminCommand({ ping: 1 }).ok\" | grep 1"]
      interval: 5s
      timeout: 5s
      retries: 20

  mongo-init:
    image: mongo:7.0.21
    depends_on:
      mongo:
        condition: service_healthy
    volumes:
      - ./backend/src/mongo/init-replica.sh:/init-replica.sh:ro
    command: ["bash", "/init-replica.sh"]
    environment:
      MONGO_HOST: mongo

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      target: artifacts
    volumes:
      - frontend_dist:/frontend-dist

  app:
    build:
      context: ./backend/src
      dockerfile: ./app/Dockerfile
    env_file:
      - ./.env
    environment:
      SECRET_KEY: ${SECRET_KEY:-dev-secret-change-me}
      MONGO_URI: ${MONGO_URI:-mongodb://mongo:27017/kriegspiel?replicaSet=rs0}
      ENVIRONMENT: ${ENVIRONMENT:-development}
      LOG_LEVEL: ${LOG_LEVEL:-info}
      SITE_ORIGIN: ${SITE_ORIGIN:-http://localhost:5173}
    depends_on:
      mongo-init:
        condition: service_completed_successfully
    expose:
      - "8000"
    ports:
      - "18000:8000"
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8000/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 12

  nginx:
    build:
      context: ./backend/src/nginx
      dockerfile: Dockerfile
    depends_on:
      frontend:
        condition: service_completed_successfully
      app:
        condition: service_healthy
    ports:
      - "18080:80"
    volumes:
      - frontend_dist:/usr/share/nginx/html:ro

  mongo-express:
    image: mongo-express:1.0.2
    profiles: ["dev"]
    depends_on:
      mongo:
        condition: service_healthy
    ports:
      - "8081:8081"
    environment:
      ME_CONFIG_MONGODB_SERVER: mongo
      ME_CONFIG_MONGODB_URL: mongodb://mongo:27017/
      ME_CONFIG_BASICAUTH_USERNAME: ${ME_USERNAME:-admin}
      ME_CONFIG_BASICAUTH_PASSWORD: ${ME_PASSWORD:-admin}

  certbot:
    image: certbot/certbot:v3.1.0
    profiles: ["prod"]
    entrypoint: ["/bin/sh", "-c", "echo certbot placeholder; sleep infinity"]

volumes:
  mongo_data:
  frontend_dist:
